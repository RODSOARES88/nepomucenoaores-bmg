# NUNCA commitar o HTML em claro — a senha StatiCrypt só protege o index.html cifrado.
# O claro vive só local (fonte de edição) e é regenerado pelo bot via --decrypt em runtime.
decrypted/
decrypted_bot/
*.claro.html

# Artefatos StatiCrypt
encrypted/

# Sistema
.DS_Store
Thumbs.db
*.swp
node_modules/

# 🔐 Credenciais Azure locais · NUNCA commitar (repo público · secret vazaria)
.env
scripts/.env
*.env
!*.env.exemplo
